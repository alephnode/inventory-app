/** @jsx jsx */
import { jsx, css } from '@emotion/core'

const addStyles = css`
  color: var(--accent-color);
  text-decoration: none;
  position: fixed;
  right: 0.75rem;
  bottom: 0.75rem;
  font-size: 1rem;
`
export { addStyles }
